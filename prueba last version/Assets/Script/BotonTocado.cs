using UnityEngine;
//using Leap.Unity;

public class BotonTocado : MonoBehaviour
{/*
    private HandController handController;

    private void Start()
    {
        // Obtén una referencia al controlador de manos
        handController = FindObjectOfType<HandController>();
    }

    private void Update()
    {
        // Obtiene todas las manos detectadas por el controlador de manos
        Frame frame = handController.GetFrame();
        HandList hands = frame.Hands;

        // Comprueba si alguna mano está cerca del botón
        foreach (Hand hand in hands)
        {
            if (Vector3.Distance(hand.PalmPosition.ToVector3(), transform.position) < 0.1f)
            {
                // Se detectó una mano cerca del botón, puedes implementar tu lógica aquí
                BotonTocado();
            }
        }
    }

    private void BotonTocado()
    {
        // Implementa aquí la lógica para cuando el botón sea tocado
        Debug.Log("Botón tocado");
        // Puedes realizar acciones como cambiar la apariencia del botón o ejecutar una animación, por ejemplo.
    }*/
}